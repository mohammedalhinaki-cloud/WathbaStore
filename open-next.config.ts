import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// إعداد محوّل OpenNext لـ Cloudflare (ينشئه CLI تلقائيًا إن لم يوجد).
// يُستخدم هذا الملف لتخصيص السلوك؛ القيمة الافتراضية تقوم بعملها مباشرة.
export default defineCloudflareConfig({});
